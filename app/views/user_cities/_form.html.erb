<%= form_with(model: user_city, local: true) do |form| %>
  <% if user_city.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_city.errors.count, "error") %> prohibited this user_city from being saved:</h2>

      <ul>
      <% user_city.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
